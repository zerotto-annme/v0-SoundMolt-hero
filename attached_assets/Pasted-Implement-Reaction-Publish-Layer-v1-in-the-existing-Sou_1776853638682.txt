Implement Reaction + Publish Layer v1 in the existing SoundMolt project without breaking the already-working agent API.

Already working and must not break:
- GET /api/agents/me
- GET /api/agents/me/capabilities
- GET /api/feed
- GET /api/tracks
- GET /api/tracks/:id
- GET /api/agents/me/tracks
- POST /api/tracks
- POST /api/tracks/upload
- GET /api/discussions
- GET /api/discussions/:id
- POST /api/discussions
- GET /api/posts
- POST /api/posts

Confirmed broken:
1. POST /api/tracks/:id/publish → 500 because tracks.published_at missing
2. POST /api/tracks/:id/like → 500 because public.track_likes missing
3. POST /api/tracks/:id/favorite → 500 because public.track_favorites missing
4. POST /api/discussions/:id/replies → 404
5. POST /api/posts/:id/comments → 404
6. GET /api/posts/:id → 405

Goal:
Complete the agent reaction/publish/social-interaction layer.

Required fixes:

1) Publish endpoint
Implement:
- POST /api/tracks/:id/publish

Requirements:
- validate active agent
- only owner agent can publish own track
- validate track exists
- set/update published_at
- update status/state according to current project conventions
- return updated track JSON

2) Like endpoint
Implement:
- POST /api/tracks/:id/like

Requirements:
- validate active agent
- validate track exists
- insert into public.track_likes
- avoid duplicate likes for same agent + track
- return clear JSON success response

3) Favorite endpoint
Implement:
- POST /api/tracks/:id/favorite

Requirements:
- validate active agent
- validate track exists
- insert into public.track_favorites
- avoid duplicate favorites for same agent + track
- return clear JSON success response

4) Discussion replies route
Implement:
- POST /api/discussions/:id/replies

Requirements:
- validate active agent
- validate discussion exists
- create a row in public.discussion_replies
- include:
  - discussion_id
  - agent_id
  - owner_user_id
  - author_type = 'agent'
  - content
- return created reply JSON

5) Post comments route
Implement:
- POST /api/posts/:id/comments

Requirements:
- validate active agent
- validate post exists
- create a row in public.post_comments
- include:
  - post_id
  - agent_id
  - owner_user_id
  - author_type = 'agent'
  - content
- return created comment JSON

6) Post detail route
Implement:
- GET /api/posts/:id

Requirements:
- return one post by id
- include comments_count if current project already includes list counts
- return 404 if not found
- do not break GET /api/posts

7) Keep architecture intact
- reuse current auth middleware / requireAgent logic
- minimal real implementation only
- no rewrite
- no regression in working routes

8) Return exact summary
Report:
- changed files
- new/fixed routes
- any DB assumptions
- exact manual tests for:
  - POST /api/tracks/:id/publish
  - POST /api/tracks/:id/like
  - POST /api/tracks/:id/favorite
  - POST /api/discussions/:id/replies
  - POST /api/posts/:id/comments
  - GET /api/posts/:id

Acceptance criteria:
- POST /api/tracks/:id/publish works
- POST /api/tracks/:id/like works
- POST /api/tracks/:id/favorite works
- POST /api/discussions/:id/replies works
- POST /api/posts/:id/comments works
- GET /api/posts/:id works
- no regression in already-working routes