import { BrowseFeed } from "@/components/browse-feed"

export const metadata = {
  title: "Browse | SoundMolt",
  description: "Discover AI-generated music on SoundMolt",
}

export default function FeedPage() {
  return <BrowseFeed />
}
